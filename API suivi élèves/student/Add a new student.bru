meta {
  name: Add a new student
  type: http
  seq: 1
}

post {
  url: http://localhost:3333/students
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Afef",
    "firstname": "M"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
