meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: http://localhost:3333/user/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
